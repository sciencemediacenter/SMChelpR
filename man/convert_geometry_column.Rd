% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/GraphQL_get_table.R
\name{convert_geometry_column}
\alias{convert_geometry_column}
\title{Convert a GraphQL geometry column to sf}
\usage{
convert_geometry_column(tbl, geom_col = "geometry", crs_default = 4326)
}
\arguments{
\item{tbl}{A tibble containing a geometry column in GraphQL format}

\item{geom_col}{The name of the geometry column (defaults to "geometry")}

\item{crs_default}{The default CRS to use if none is found in the data (defaults to 4326)}
}
\value{
A tibble converted to an sf object with proper geometry
}
\description{
Takes a tibble with a geometry column from GraphQL and converts it to a simple features (sf) object.
The function handles the JSON structure that GraphQL returns for geometries.
}
\examples{
# Pipe-friendly usage
polygons_sf <- GraphQL_get_table_vec(
  tabellenname = "spatial_data",
  variablen = c("id", "name", "geometry")
) |>
  convert_geometry_column()
}
