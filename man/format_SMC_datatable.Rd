% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/format_SMC.R
\name{format_SMC_datatable}
\alias{format_SMC_datatable}
\title{format_SMC_datatable}
\usage{
format_SMC_datatable(
  table,
  columns,
  digits = 1,
  decimal_mark = ",",
  big_mark = "thin_space",
  na_text = "keine Daten"
)
}
\arguments{
\item{table}{a \code{datatable} htmlwidget created with \code{\link[DT:datatable]{DT::datatable()}}.}

\item{columns}{character, names of the columns to format (matched against
the column names of the table data).}

\item{digits}{integer or NULL, number of decimals. An integer (default \code{1})
forces that number of decimals for every value; \code{NULL} uses 0 decimals for
whole numbers and 1 decimal otherwise.}

\item{decimal_mark}{character, decimal mark used in the displayed value.
Defaults to ",".}

\item{big_mark}{character, thousands separator. The sentinel "thin_space"
(default) inserts a narrow 50\% font-size HTML space; any other string is
used verbatim.}

\item{na_text}{character, text shown for \code{NA}/\code{null} cells. Defaults to
"keine Daten".}
}
\value{
the \code{datatable} object with an added column render definition.
}
\description{
Apply SMC-style number formatting to one or more columns of a \code{DT::datatable}
object. The formatting is performed client-side via a DataTables render
callback, so it only affects the displayed value (comma as decimal mark,
narrow space as thousands separator) while sorting and filtering keep working
on the underlying numeric data. \code{NA}/\code{null} cells are rendered as \code{na_text}.
}
\details{
By default (\code{digits = 1}) every value is shown with one decimal. Pass
\code{digits = NULL} for a dynamic mode that shows whole numbers without decimals
and non-integers with one decimal.
}
\examples{
library(DT)
df <- data.frame(Energietraeger = c("Wind", "Solar"), Leistung = c(1234.5, 980))
datatable(df, rownames = FALSE) |>
  format_SMC_datatable(columns = "Leistung")
}
