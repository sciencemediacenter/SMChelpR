% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/shiny_download_SMC.R
\name{image_download_observer}
\alias{image_download_observer}
\title{image_download_observer}
\usage{
image_download_observer(input, session, id_image, output_id, filename)
}
\arguments{
\item{input, session}{the module server's \code{input}/\code{session}.}

\item{id_image}{base id as passed to \code{\link[=caption_download_line]{caption_download_line()}}
(without namespace).}

\item{output_id}{id of the \code{echarts4rOutput} (without namespace).}

\item{filename}{function returning the file name without extension; the
download date and format are appended.}
}
\value{
Called for its side effect of registering two
\code{\link[shiny:observeEvent]{shiny::observeEvent()}}s.
}
\description{
Wires up the client-side PNG/SVG downloads of a chart: clicking the
actionLinks from \code{\link[=caption_download_line]{caption_download_line()}} sends the output id, format
and file name as a custom message to the browser, where the bundled JS
asset exports the live ECharts instance via \code{getDataURL()} — WYSIWYG
including the current zoom and legend state, without any server-side
rendering. The chart must run the SVG renderer (\code{e_charts(renderer = "svg")}); the PNG is rasterized client-side from the SVG at 3x
resolution.
}
