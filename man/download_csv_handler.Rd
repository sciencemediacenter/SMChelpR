% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/shiny_download_SMC.R
\name{download_csv_handler}
\alias{download_csv_handler}
\title{download_csv_handler}
\usage{
download_csv_handler(filename, data)
}
\arguments{
\item{filename}{function returning the file name without extension
(e.g. built with \code{\link[=filename_slug]{filename_slug()}} from the current selection); the
download date and ".csv" are appended.}

\item{data}{function returning the data.frame to export.}
}
\value{
A \code{\link[shiny:downloadHandler]{shiny::downloadHandler()}}.
}
\description{
\code{\link[shiny:downloadHandler]{shiny::downloadHandler()}} for a CSV export generated on the fly: use
the same data function that feeds the chart, so download and figure can
never drift apart and the current filter state is exported. Column
names are cleaned via \code{\link[=csv_clean_column_names]{csv_clean_column_names()}}; while no data is
available (backend unreachable, fetch still running), \code{\link[shiny:req]{shiny::req()}}
aborts the download.
}
