% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/export_mapgl.R
\name{export_mapgl_png_html}
\alias{export_mapgl_png_html}
\title{export_mapgl_png_html}
\usage{
export_mapgl_png_html(
  map,
  pngpfad,
  htmlpfad,
  vwidth = 800,
  vheight = 600,
  delay = NULL,
  include_legend = TRUE,
  hide_controls = TRUE,
  background = "white"
)
}
\arguments{
\item{map}{a \code{mapgl} widget (from \code{\link[mapgl:maplibre]{mapgl::maplibre()}} or \code{\link[mapgl:mapboxgl]{mapgl::mapboxgl()}}).}

\item{pngpfad}{character, output path for the PNG file.}

\item{htmlpfad}{character, output path for the self-contained HTML file.}

\item{vwidth}{integer, viewport width in px — also the PNG width. Default: 800.}

\item{vheight}{integer, viewport height in px — also the PNG height.
Default: 600.}

\item{delay}{numeric or \code{NULL}, optional extra seconds to wait after the map
reports idle before the screenshot — a fallback for stubborn remote
basemaps that never fully settle on headless Linux. Default: \code{NULL} (rely on
the idle wait only).}

\item{include_legend}{logical, keep \code{mapgl} legends in the PNG. Default: \code{TRUE}.}

\item{hide_controls}{logical, hide navigation/fullscreen/etc. controls in the
PNG (interactive-only chrome). Default: \code{TRUE}.}

\item{background}{character, background colour behind the map in the PNG.
Default: \code{"white"}.}
}
\value{
Invisibly \code{NULL}. Called for its side effect of writing the two files.
}
\description{
Export a \code{mapgl} map (a MapLibre GL / Mapbox GL widget) to two files: a
self-contained interactive HTML version and a static PNG. The map counterpart
to \code{\link[=export_echart_png_html_svg]{export_echart_png_html_svg()}}.
}
\details{
Two things differ from the echarts exporter, both because MapLibre draws into
a WebGL canvas rather than an SVG DOM:
\itemize{
\item \strong{No SVG.} There is no \verb{<svg>} node to extract, so a static export is
always a raster screenshot. If you need true vector/print output, use QGIS.
\item \strong{The PNG capture is delegated to \code{\link[mapgl:save_map]{mapgl::save_map()}}}, which serves the
widget over a temporary local HTTP server and screenshots it in a headless
Chrome session (\pkg{chromote}). Serving over HTTP rather than from a
\verb{file://} URL is what lets range-request tile sources — PMTiles data tiles,
remote basemaps — actually load during the capture. \code{save_map()} waits for
the map to reach an idle/rendered state before shooting, so the timing is
deterministic instead of a fixed delay.
}

\strong{Resolution = viewport.} The PNG is \code{vwidth} by \code{vheight} pixels. There is
deliberately no device-scale/\code{scale} argument: MapLibre sizes its WebGL canvas
at map-creation time, so raising a device-scale factor \emph{after} the page has
loaded (as \code{save_map()} does) does not upscale the map on headless Linux — it
is a no-op there (verified with mapgl 0.5.0). For a larger/sharper PNG, render
at a larger viewport and adjust \code{zoom}/\code{center}/\code{maxBounds} to keep the
framing, rather than reaching for a pixel-ratio knob.

The self-contained HTML embeds the widget itself but still fetches tiles
(basemap style, PMTiles) from their URLs at view time — the interactive map
is the report's main artefact, the PNG the print fallback.

Requires the suggested packages \pkg{mapgl} (>= 0.5.0, for \code{save_map()}) and
\pkg{chromote} (needs a headless Chrome/Chromium installation); they are
checked at runtime. On CI/headless machines without a GPU, Chrome needs
software WebGL (e.g. the \code{--enable-unsafe-swiftshader} flag) or the map
renders blank.
}
\examples{
\dontrun{
library(mapgl)
m <- maplibre(carto_style("positron"), center = c(10.4, 51.2), zoom = 5)
export_mapgl_png_html(
  m,
  pngpfad = file.path(tempdir(), "karte.png"),
  htmlpfad = file.path(tempdir(), "karte.html")
)
}
}
\seealso{
\code{\link[=export_echart_png_html_svg]{export_echart_png_html_svg()}} for the echarts equivalent.
}
