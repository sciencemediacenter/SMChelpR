% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/format_SMC.R
\name{format_SMC_number}
\alias{format_SMC_number}
\title{format_SMC_number}
\usage{
format_SMC_number(
  x,
  digits = 1,
  decimal_mark = ",",
  big_mark = "thin_space",
  na_text = "keine Daten"
)
}
\arguments{
\item{x}{numeric, value or vector to be formatted.}

\item{digits}{integer, number of decimals used for non-integer values
(whole numbers are always shown without decimals). Defaults to 1.}

\item{decimal_mark}{character, decimal mark used in the output. Defaults to ",".}

\item{big_mark}{character, thousands separator. The sentinel "thin_space"
(default) inserts a narrow 50\% font-size HTML space; any other string is
used verbatim.}

\item{na_text}{character, text used for \code{NA} values. Defaults to "keine Daten".}
}
\value{
character vector of the same length as \code{x} with the formatted values.
}
\description{
Format a numeric value (or vector) as a string in SMC style for use in
inline text or rendered HTML. By default a comma is used as the decimal mark
and a narrow (50\% font-size) HTML space as the thousands separator, both
configurable via \code{decimal_mark} and \code{big_mark}. Whole numbers are shown
without decimals, non-integers with \code{digits} decimals. \code{NA} is rendered as
\code{na_text}.
}
\examples{
format_SMC_number(105.526)
format_SMC_number(c(100, 1234.5, NA), digits = 1)
format_SMC_number(1234.5, big_mark = ".", decimal_mark = ",")
}
