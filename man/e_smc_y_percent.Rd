% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/echarts4r_SMC.R
\name{e_smc_y_percent}
\alias{e_smc_y_percent}
\title{e_smc_y_percent}
\usage{
e_smc_y_percent(
  e,
  name = NULL,
  extend_to = NULL,
  interval = NULL,
  echarts_params = list()
)
}
\arguments{
\item{e}{an \code{echarts4r} chart.}

\item{name}{character or \code{NULL}. Vertical axis title.}

\item{extend_to}{numeric or \code{NULL}. Minimum value the axis must reach
(e.g. 105 so a 100 \% line stays visible).}

\item{interval}{numeric or \code{NULL}. Fixed tick interval; defaults to 20
when \code{extend_to} is set.}

\item{echarts_params}{list, selective overrides of the style constants
(see \code{\link[=get_SMC_echarts_default_parameters]{get_SMC_echarts_default_parameters()}}).}
}
\value{
The modified \code{echarts4r} chart.
}
\description{
Percent y axis in the SMC style: labels as \code{"{value} \%"} and an optional
vertical axis title (\code{nameLocation = "middle"}).
}
\details{
\code{extend_to} guarantees that the axis covers at least this value — use it
when a reference line (e.g. the 100 \% mark via \code{\link[=e_smc_hline]{e_smc_hline()}}) may lie
above the data maximum: ECharts does not extend the axis for mark lines,
so without it the line would be clipped. The axis maximum is rounded up
to the next multiple of \code{interval}.
}
